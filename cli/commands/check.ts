// ocp check——机械检查项路由 stub（块C 重写填充：whitespace / line-budget / commit-format /
// transition-consistency / platform-words 五子命令）。路由能到、编译能过即可。
export async function main(): Promise<number> {
  console.error("块C 交付");
  process.exit(2);
}
