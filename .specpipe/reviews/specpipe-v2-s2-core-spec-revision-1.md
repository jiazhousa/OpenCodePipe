# 审查报告: specpipe-v2-s2-core (Revision 1)

## 总体评价
通过。四部件与 epic-spec Story 2 / roadmap 2a-2d 对齐，契约引用准确，无致命遗漏；下列问题供 S-S6 吸收，不阻塞放行。

## 分项意见
1. **范围对齐** — 通过：2a/2b/2c/2d 与 roadmap 部件表逐项对应（单包决策、1.18.*+lockfile+冒烟、数据文件共消费、stage 双工具、五角色迁移）；「不做」项与 S3/S4/S5 切分清晰；2 处偏差见问题 1/2。
2. **致命遗漏** — 通过：验收 1/3 可核验（fence 全绿；三情形手验+截图/会话记录留证）；验收 2「全部合法序列+代表性非法」建议 impl 收敛口径（每条合法边≥1 用例、三路径端到端各 1 条、非法转移按状态枚举）；验收 5 建议定留证形式（diff 清单存档供 S3e 基线）。
3. **契约一致性** — 通过：07 卷 5 类审查转移、放行边、`{wf}/plans/{topic}/` 路径、JSONL 五字段与 actor 集合逐项准确；08 卷五角色/权限边界/模型决策分离表述准确。OVERTURN 边在 07 卷为叙述性文字，建议 impl 将审查结论（PASS/REJECT/REJECT:SPEC_OVERTURN）作为边标识纳入数据文件并在验收 2 覆盖该边。

## 问题清单
1. **权限白名单「生效单测」降级未声明（medium）**：epic-spec Story 2 单测含「权限生效」、roadmap 2d 验收含「白名单生效单测」，spec 验收 4 仅「diff 核对」且无降级依据。建议补最小自动化校验（解析五文件断言权限段/白名单结构）或显式记录降级理由与替代验证（S4 实跑）。
2. **CI 条款上游档案漂移（low）**：epic-spec Story 2 / roadmap 2a 仍含「CI（job）」，spec 规则 7 为「暂不建（用户裁决）」。建议同步修订上游档案或标注「对 Epic 的显式修订点」，避免 S4 终检歧义。
3. **Epic 状态数表述歧义（low）**：「Epic 5 + ALL_DONE」与「Story 10 / Issue 6」含终态写法不同构（07 卷 Epic 链共 5 状态含 ALL_DONE），易读成 6 个。建议改「Epic 5（含 ALL_DONE）」。
4. **数据文件完备性边界待明确（low）**：① 未明确「无 .stage → *_DRAFT」建档边及留痕（07 卷「调度者负责 DRAFT 创建」+中断恢复「无 .stage」情形）；② epic 规则 2 所列「阻塞语义 / S2 重分级 / 中断恢复状态处理」在数据文件中的取舍未交代。建议 impl 阶段明确这两处表达。

## 结论
# PASS
状态：SPEC_REVIEWING → SPEC_USER_AUDIT
